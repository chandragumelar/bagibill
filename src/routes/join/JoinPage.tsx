import { t } from "@/lib/i18n";

// Kerangka F0-06 — cabang bundle terpisah, disiapkan tapi belum dijadwalkan
// (gelombang 2). Isi tetap kosong sampai join grup masuk plan.md.
export default function JoinPage() {
  return (
    <main>
      <p>{t("route.placeholder")}</p>
    </main>
  );
}
